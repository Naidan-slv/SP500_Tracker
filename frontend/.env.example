# Optional override. For local dev, app defaults to /api (Vite proxy) to avoid CORS.
# For production frontend deployments, set this to your API base URL.
# VITE_API_BASE_URL=https://sp500-tracker.onrender.com
